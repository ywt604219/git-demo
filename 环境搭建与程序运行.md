# 环境搭建与程序运行

## task1

### 1

jvm是运行 Java 程序的"发动机"

JRE=JVM + 现成的零件库 ， 能运行 Java 程序

JDK=JRE + 工具（编译器 javac 等）=，能开发并运行 Java 程序。

### 2

JDK 里有 `javac`，把 `Hello.java` 翻译成 `Hello.class`

JVM 读取 `Hello.class`，翻译成电脑能懂的指令并执行。

### 3,4

![](C:\Users\iyt\Desktop\工作室.png)

## task2

### 1

Path 里加了 JDK 的 bin 目录

### 2

输入 `java` 时，系统会到 Path 里的每个目录去找 `java.exe`。

加了 `%JAVA_HOME%\bin` 后，系统在这个目录找到了 `java.exe` 和 `javac.exe`，所以能直接运行。

### 3.4

`javac` 负责**翻译**，`java` 负责**运行**

(emmm最终还是成功了)

![](C:\Users\iyt\Desktop\2.png)

## task3

部分：包声明、import、主类、main 入口、其他

包：类的文件夹，分类 + 防重名

为什么要包:组织代码、避免冲突、权限控制

import:少写全名，方便引用其他包的类

main:签名由 Java 规范固定，JVM only

## task4

![](C:\Users\iyt\Desktop\3.png)

![](C:\Users\iyt\Desktop\4.png)

`src` 下按包名建目录，包名和目录一一对应；`HelloWorld` 在 `com.Example` 包，`Print` 在 `com.Example.tool` 包；`HelloWorld` 调用 `Print`， import 进来调用


├── src/                      ← 源代码目录
│   └── com.Example/
│       ├── HelloWorld.java   ← 主类，包名 com.Example
│       └── tool/
│           └── Print.java    ← 工具类，包名 com.Example.tool

### 我的困难

不不不我无疑是收获极大的。今天学会了markdown语法eeee还有git仓库使用方法（还不小心交了一个空白的上去eeee）

还有从零开始下java配环境，一开始很迷茫。什么都看不懂。后面烧到方法了去查方法教程貌似也能慢慢学下去。虽然现在依然一知半解但是已经进步非常大了（）eeee好歹也是学会了怎么下载怎么配环境怎么用仓库。有的步骤记不清像markdown语法还有vscode和git配合，不过我相信在后面的反复使用（鞭策）下一定会熟练掌握的。

另外：

十分万分郑重感谢工作室，给了我学习的方向和学习的动力

